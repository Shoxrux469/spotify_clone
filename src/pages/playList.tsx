import { useLocation } from "react-router";

const PlayListPage = () => {
  const { state } = useLocation();
  console.log(state);

  return (
    <div>
      <div>

      </div>
    </div>
  );
};
export default PlayListPage;
